# Image Skill TEST_PREP

Deterministic tests: request serialization/seed/dimensions/format and pinned fake-provider artifact SHA. Malformed tests: blank/oversized prompt, invalid dimensions/format/seed, corrupt bytes. Provider tests: absent -> `needs_provider`; timeout, exception, cancellation, empty artifact, wrong MIME, oversized output, metadata mismatch all fail closed with no success claim. A real adapter must independently prove decoded image dimensions/format and artifact digest; provider metadata alone is insufficient. Reproducible fixtures must include valid PNG/JPEG plus corrupt/truncated cases. Measure latency, peak RAM, output/storage bytes, retry count. Assert prompt/provider metadata retention is explicit, secrets never logged, and artifact provenance binds prompt digest + adapter/version. Android adapter tests must cover lifecycle cancellation/backgrounding and bounded memory. Rollback/disable: remove adapter registration; planner remains usable but generation unavailable.