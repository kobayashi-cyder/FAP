name: Horizontal plan quality

on:
  push:
    branches:
      - horiz/coding-plan-quality
  pull_request:
    paths:
      - "fap_repository_plan_quality.py"
      - "tests/test_fap_repository_plan_quality.py"
      - ".github/workflows/horiz-plan-quality.yml"

jobs:
  verify:
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m compileall -q fap_repository_plan_quality.py
      - run: python -m unittest tests.test_fap_repository_plan_quality -v
