name: Horizontal diff minimizer

on:
  push:
    branches:
      - horiz/coding-diff-minimizer
  pull_request:
    paths:
      - "fap_repository_diff_minimizer.py"
      - "tests/test_fap_repository_diff_minimizer.py"
      - ".github/workflows/horiz-diff-minimizer.yml"

jobs:
  verify:
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m compileall -q fap_repository_diff_minimizer.py
      - run: python -m unittest tests.test_fap_repository_diff_minimizer -v
