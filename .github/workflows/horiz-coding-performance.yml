name: Horizontal coding performance

on:
  push:
    branches:
      - horiz/coding-performance
  pull_request:
    paths:
      - "fap_repository_performance.py"
      - "tests/test_fap_repository_performance.py"
      - ".github/workflows/horiz-coding-performance.yml"

jobs:
  verify:
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m compileall -q fap_repository_performance.py
      - run: python -m unittest tests.test_fap_repository_performance -v
