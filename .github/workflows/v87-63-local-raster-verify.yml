name: V87.63 Local Raster Verify

on:
  push:
    paths:
      - "fap_local_image_backend.py"
      - "fap_image_orchestrator.py"
      - "fap_v87_63_local_raster_gateway.py"
      - "fap_v87_62_semantic_action_gateway.py"
      - "fap_v87_12_semantic_adaptive_gateway.py"
      - "fap_v87_10_program_synth_gateway.py"
      - "knowledge/visual_concepts_ja.jsonl"
      - "tests/test_local_raster_image.py"
      - "RUN_FAP_CHAT_LATEST.sh"
      - "RUN_FAP_CHAT_LATEST.ps1"
      - ".github/workflows/v87-63-local-raster-verify.yml"
  pull_request:
    paths:
      - "fap_local_image_backend.py"
      - "fap_image_orchestrator.py"
      - "fap_v87_63_local_raster_gateway.py"
      - "fap_v87_62_semantic_action_gateway.py"
      - "fap_v87_12_semantic_adaptive_gateway.py"
      - "fap_v87_10_program_synth_gateway.py"
      - "knowledge/visual_concepts_ja.jsonl"
      - "tests/test_local_raster_image.py"
      - "RUN_FAP_CHAT_LATEST.sh"
      - "RUN_FAP_CHAT_LATEST.ps1"
      - ".github/workflows/v87-63-local-raster-verify.yml"
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Check launcher syntax
        run: sh -n RUN_FAP_CHAT_LATEST.sh
      - name: Compile local image stack
        run: |
          python -m py_compile fap_local_image_backend.py fap_image_orchestrator.py fap_v87_63_local_raster_gateway.py
      - name: Safe-mode gateway preflight
        run: |
          PYTHONNOUSERSITE=1 PYTHONDONTWRITEBYTECODE=1 python -S -B -c 'import fap_v87_63_local_raster_gateway as g; s=g.CORE.chat_status(); assert s.get("version") == "87.63-unified-chat", s'
      - name: Run local raster regression tests
        run: python -m unittest tests.test_local_raster_image -v
