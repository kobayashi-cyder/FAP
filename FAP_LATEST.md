# FAP V1.0.01 public mainline

Canonical runtime: `fap_runtime_1x.py`.

The active runtime does not import the previous numbered gateway chain.
Unknown local requests fail closed rather than fabricating an answer.

Evidence:
- dynamic-sparse routing: unit + mechanism benchmark;
- bounded compute: unit regressions;
- speech: unit/device-abstraction tests, unrestricted dictation unproven;
- Android: compile-level verification, real-device verification pending;
- native C++: build/unit/CLI smoke, full parity partial;
- browser self-improvement: isolated tests, authenticated real-browser evidence pending.

GPT-5.6 Sol-class performance remains unverified at model level.
